# Jira URL (z.B. https://ihre-firma.atlassian.net oder https://jira.firma.net/jira)
JIRA_URL=https://jira.firma.net/jira

# Authentifizierungstyp: "bearer" für Personal Access Token (PAT) oder "basic" für Cloud API Token
JIRA_AUTH_TYPE=bearer

# Ihr Jira Personal Access Token (PAT) oder API Token
# Für Data Center/Server: Erstellen Sie einen PAT in Ihren Jira-Profileinstellungen
# Für Cloud: Erstellen Sie einen API Token unter https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_PAT=

# Nur für JIRA_AUTH_TYPE=basic (Atlassian Cloud):
# JIRA_EMAIL=ihre.email@firma.de
# Eigenen JQL schreiben. Als Standard wird diese verwendet: 
JIRA_JQL='assignee = currentUser() ORDER BY updated DESC'